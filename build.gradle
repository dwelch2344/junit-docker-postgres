plugins {
    id 'java'
}

repositories {
    mavenCentral();
}

dependencies {
    compile 'junit:junit:4.12'
    compile 'com.spotify:docker-client:3.6.8'
    compile 'org.postgresql:postgresql:9.4.1208.jre7'
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.11'
}